---
### 自我介绍
- 好的面试官，首先非常感谢xxx能够给我这样一次面试机会，我是本次的面试者xxx，来自xxx大学xxx专业的本科生
- 在校期间，我学习了java，golong等编程语言，有扎实的编程基础，也学习过常见的后端、agent的组件与框架，了解一些分布式思想，同时作为科班出身，我也有扎实的计算机基础，学习过计算机网络、操作系统、数据库、数据结构这类核心课程
- 在前两个月的时间里，我在杭州一家公司有一段实习经历，最开始是参与了xxxAPP里智能相册模块的开发，主要是去做一些工程方案的落地以及测试验证，后来因为小团队内项目排期不够透明，我就被指派去独立负责了一个基于钉钉im的ai项目管理工具，是一个内部工具，打通了从开发到部署的闭环，帮助团队提效
- 此外在日常学习当中，我比较喜欢去实践自己的一些思考，重点钻研了两个项目
- 一个是我从零到一开发的分布式的即时通信系统，也是im系统，其中比较有挑战的是，做系统整体架构的解耦、以及解决分布式里的各种极端异常情况
- 另一个是面向c端的agent服务平台，比较有挑战的是，结合agent工程和后端工程，去设计了一些agent harness相关的治理
- 具体的细节在简历中都有体现
- 最后在ai时代，我认为自己能够用好ai工具，并且利用ai工具去做到软件开发的提效，非常希望能够得到咱们团队的认可，以上就是我的基本信息，谢谢面试官
### 实习介绍
#### 为什么短时间离职
- 学校有个实训必须要报个到
- 阶段性项目已经交付
- 团队其他核心模块不好拆分，也不太需要实习生参与
- 积累了很宝贵的工作经验，但还是想去更大的平台发展
#### 为什么要做优化
- ld觉得成集效果不好
- 算法方案是业务负责人定下的，目标是把优化后的链路先跑通
- 因此我的职责主要还是去做还是工程上的落地与验证
#### 讲讲L1L2L2.5是做什么的
- 总体需求是，用户上传图片当作聊天消息，系统自动给照片归属相册集
- L1是图片的视觉分析，提取出图片的元数据metadata json
- L2是判断用户上传的最多9张图片能不能成集，现阶段产品的预设是用户在交互直觉上会倾向于上传一类图片，所以只判断成集与不成集两种结果，但也要预留出成多集和部分成集的底层逻辑
- 如果用户一次性上传的图片小于3张，就会尝试从沉淀池里捞出老图片凑成3张，捞不出来就走L2.5
- 如果L2流程未成集，也走L2.5
- L2.5就是每张图片判断能不能并到已有相册集里，如果还并不了就进沉淀池
#### 讲讲L2的规则
- 采用了多维打分+真值表统一判决的方式
- 主维度是Location、Time、Event，辅维度是People、Anchor（向量）、Emotional（枚举）
- 每个维度经过各自的打分方法后映射到strong，medium，light
- 再通过真值表判断是否成集，三主strong/两主strong+一主medium+一辅strong/一主strong+两主medium+两辅strong/三主medium+三辅strong
#### 讲讲event维度算法流程
- 之前只靠event去做聚类，聚类后，按照最大类中图片数量占总体图片数量的比例，进行打分，再映射到strong，medium，light三个挡位上，但event是个枚举，比较生硬
- activity是event下一个更具体的字符串字段，比较灵活，在event判断为strong时触发
- 我主要的工作是做activity邻接表的构建
- 先用向量模型（text-embedding-v4 1024维）把activity向量化
- 计算余弦相似度矩阵，构建出邻接矩阵（时间n²d，空间n² double[][]）
- 排序（时间n² log n）
- 检查双向近邻，构建出邻接表（时间nk，空间nk，List<Map<Integer, Double>>），n=3-5时k=2，n=6-9时k=3
- 这里要过滤掉余弦相似度小于0.2的，并且把剩余的边归一化，不然弱边比强边的数值会更高，后续Louvain中容易形成更大的社区
- 这里不用KNN单向近邻是因为，如果一张图是独立点也会去强行找相邻的点连线
- 之后的Louvain聚类我没有具体负责，但我大概清楚它是遍历每一个节点去做收缩判断，直到没有收益或者遍历次数达到上限，输出一个List<Set<Integer>>
- 这里不用K-Means是因为不好确定中心点个数k，不用DBSCAN是因为不好确定聚合最小半径
#### 讲讲L2.5的规则
- 就是之前L2阶段的Location、Time、Event三个主维度
- 比如event维度，之前不是有过聚类吗，就需要判断新图的activity和相册最大类中图片的activity的平均值是否相近，这里取的是大于0.6，就标记这个维度是符合的，如果有两个维度是符合的，我们就并入
- 但这里的规则过于主观了，所以加了llm二次判断
- 判断会发相册标题和描述，这个是L2成集收尾时有一步lmm提取的，当然还要发要并入的图片的元数据
- 当然上面说的三维判断是在锁外，也就是两张照片判断是否并入一个相册集的时候，用的都是统一的旧三维，并不会因为一方的并入而更新，产品认为这一点偏差是可以容忍的
#### 讲讲怎么做测试验证的
- 公司没有数据中台和标注团队
- 所以这里的测试确实是指工程上的验证，并不是具体的百分比的指标
- 我首先构建了一些黄金测试集，从网上找来还有爬来一些真实照片，人工修改照片的exif中的时间和gps，分为正例反例和争议用例，在改动前后的流程上做对照测试，如果原本稳定输出与预期相反的用例，而现在能稳定输出预期，就判定是有效的
- 当然我还会抽取线上日志中真实的照片用例，以及在开发环境内部体验时遇到的bug用例来做判断
#### 讲讲分布式锁和乐观锁
- 产品强调相册集的轻量化和故事感，数量要有上限
- 前面L2未成集的照片会进入分散进入mq，L2.5各自消费会有并发风险
- 只用redis分布式锁的话，风险是固定的ttl后还没入库成功
- 也讨论过看门狗的方案，但看门狗本质是起一个线程去续期，比较重，而且仍有业务假死锁不释放的风险
- 所以最终引入了数据库乐观锁兜底，并入时建一个事务，先where count<=30，如果不成功就返回，成功就改相册集三维和照片的所属相册集字段
- 当然也可以只用数据库乐观锁，但考虑到数据库请求比较重，而且本来就有分布锁的实现，还有其他的用途不好动旧逻辑
### 项目介绍一
- 受到了qq微信这类即时通信系统的启发
- 最开始，java，netty去做，因为是熟悉的领域，实现了大部分业务层的功能
- 重量、单体，且业务层和接入层耦合
- 正好学习go，了解到runtime中的netpoll与协程调度，和接入层这类重网络io的场景相性很好，尝试手搓
- 因此我重点开发两条路线
- 首先，系统作为消息的中转站，负责把消息从发送者转发到接收者，当然也有p2p这样的手段去打通通信双方的内网，但这是特殊情况下的技术考量，先不重点去谈，既然是中转站，业务层需要知道消息的接收者是否在线，也就是说是否有连接挂在接入层上，如果有又怎么发现是哪台接入层，这里其实有几种解决方案，我最终采用的是设计redis路由表，然后重点考虑接入层怎样实时同步用户在线信息
- 第二，系统负责消息的落库，也就是消息的持久化，这是无论用户在线与否，比如用户下线一段时间再上线，我们肯定要到数据库里拉取离线期间所收到的消息对吧，而且就算用户在线，我们也要为实时推送这条路的不确定性去做保证，也就是保证数据的最终一致性，这就重点需要关注消息的落库形式，比如表的设计，以及我们这里使用的异步策略
#### 三种转发模型
- 业务层查路由
- 接入层筛路由
- 根据userId固定接入层，但lvs只是四层代理，七层代理的话那接入层就作废了，接入层可能数据倾斜，单点压力大
#### 讲讲长连接与短连接
- http短连接，低频的页面访问
- http长连接，多轮请求响应，通过复用tcp连接；gpc，数据库连接池
- sse，单工，本质上是拉长了响应，用\n\n做区分，服务端可以发冒号:为首的数据块保活
#### 为什么不用websocket处理上行消息
- 后端需要解析websocket内容去做路由，而不能直接用http的url路径，当然也有stomp这样的框架去实现，这里不考虑
- 前端需要用map+promise维护消息发送的请求，收到后端响应为成功后fulfilled，超时或收到后端响应为失败后rejected重试
- 解耦，http直接融入旧架构，统一管理，享受现有基建的限流策略和检测链路
- 而且登录注册，大文件传输依旧要用http（现有解决方案成熟），离线拉取（不太需要即时性）
- 但也有缺点，http的握手开销大，对即时性不太友好
#### 为什么不用sse
- 只能做服务端心跳：[[面试话术#### 为什么是客户端心跳]]
- 拓展性不高，未来可能考虑websocket上行
- 本质还是请求响应，而不是单纯的响应推送
#### 为什么不用mqtt
- 需要mqtt brocker，就完全不是websocket网关了
- 只能做发布订阅，没法路由，没法落库
- mqtt是直接基于tcp的，浏览器不支持裸tcp，还是得mqtt over websocket（websocket先要http get）
#### 讲讲redis双层路由表
- 多设备需求
- hash，主key存userId，key存deviceId，value存nodeId
- 在没有hash内部ttl下的情况，怎么替代
- 介绍zset查deviceId，内存拼接，再string查网关
- 讲单string的坏处，scan
- 讲zset的分数好处
- 讲单zset的坏处，同deviceId顶路由这种特殊情况不好处理
#### 讲讲ttl
- 首先连接安全断开时会清理掉，ttl防止意外断开
- hash每条单独设，整体也设ttl
- zset每条单独设score，整体也设ttl
- string
- 无论是删除开始续期，都lua脚本对比connectId，如果不对再反向同步本地map
#### 讲讲本地map
- 为什么不用sync.map，删除时dirty map残留，dirty map晋升停顿
- 为什么不用读写锁，分段足够细时读写锁的原子计数反而开销大，且map内查询很快
- 介绍分段锁细节，与运算取模，二层拷贝
#### 讲讲netpoll
- 先介绍一个websocket对应一个goroutine
- epollcreate，红黑树，监听socket
- epollctl，增删改fd，注册事件
- epollwait，传参读事件或写事件，一般写事件只有缓冲区满了才去注册，双向链表，复制到用户态数组，唤醒
- 水平触发，只要缓冲区没读完每次epollwait都返回事件，频繁检查；边缘触发，只在事件变动后epollwait才返回事件
- select，poll，增删改代价大，查需要遍历，打标记全量复制回用户态，再次遍历
#### 讲讲时钟轮
- 心跳，发送者能发出说明对方在线，接受者收到说明对方在线
- [[面试话术#### 为什么是客户端心跳]]
- 底层是一个数组加链表
- 上锁取链表，go func锁外检查，isclose为true说明连接主动断开，或者redis检查时connectionId不对被断开，从链表中踢出
- 60,45,30,15检查，45,30,15,60发心跳
- 如果系统负载延迟了几秒，通过tick计数去追赶
#### 为什么是客户端心跳
- 服务端发心跳，增加下行压力，读比写压力小
- 客户端没法主动断开，服务端只能等心跳判定死亡，而服务端一般不会主动断开
#### 讲讲mq的作用
- 防下游抖动，群聊，削峰填谷，异步
- 如果是rpc，接入层抖动，会阻塞业务层，mq相比较而言没那么容易抖动
- 其次群聊用fanout避免写放大
- 对于接入层筛选群成员，以及业务层消费者对群成员分别落库，很耗时，大量群聊消息时削峰，平时单聊消息时填谷
- 当然对于业务侧落库，肯定要用异步，不能阻塞即时消息转发的逻辑
#### 讲讲mq的监控指标，出了问题如何定位，如何降级
- 消息堆积量，生产消费速率，grafana监控
- 查看上下游日志，定位投递失败，定位慢sql
- 扩容，减少重试次数，死信队列
#### 讲讲mq重试怎么考虑的
- rabbitmq分交换机失败和路由失败
- 对于路由失败，统一不管
- 对于即时链路层，生产者重试1次，非持久化，消费者不重试
- 对于落库链路层，生产者重试3次，持久化，消费者重试3次
- 消费者重试要用msgid做幂等，反复重试对群聊场景不友好，因为是整体重试，所以大群聊要读扩散
#### 为什么不用kafka
- rabbitmq由erlang语言开发，延迟性低
- kafka实现fanout比较麻烦
- 如果吞吐量大，确实需要用kafka
- kafka是0拷贝，且os处理刷盘，集群兜底
#### 讲讲etcd的作用
- 在没有mq用rpc时能做服务发现
- 业务侧去申请雪花id中的workId
- 接入层去申请mq direct的路由键，也可以直接用租约id
- 申请过程要进过cas，类似redis里的setnx，但redis对于数据一致性保障不够
#### 讲讲只增事件流
- 避免高频修改的行锁
- 如果游标更新，修改旧数据页无意义
- 后续还可以自定义消息type，预留jsonb字段追加extradata
#### 讲讲数据库表设计
- 两张表对应读扩散、写扩散
- 雪花id配合userId和groupId方便分库分表
- 为什么不能纯写，因为写放大，更重要的是，写扩散的局部失败会导致整体重试（顺带引出幂等兜底）
- 拉取离线消息逻辑，联合索引，userId+cursor一次性拉个人邮箱表，groupId+cursor多次拉大群聊消息
#### 讲讲双id体系
- 只用雪花id的话，没法判断空洞，假设一种情况，12ms的雪花id比11ms的雪花id先落库，在期间正好触发拉取，前端永久更新游标为12ms
- 还有一种情况，在建立连接成功后，极短时间内是还没有被同步到本地map和redis中的，此时即时消息是收不到的，但离线拉取已经完成了
- 只用seqid的话，首先宏观上看，是一张表，不能用seqId做主键，那如果是seqId+userId的联合索引做主键呢，虽然正好匹配拉取需求，但幂等没法保障，这里就可以用msgid做申请seqId的去重键
- 其次在即时消息转发链路上，seqId是还没有申请的，需要msgId保证前端展示的顺序
#### 讲讲redis状态机
- 有3个api，初始化，上传，合并
- 初始化，判断是否秒传，判断是否有用户在传，申请key同时写redis，如果是断点就返回共用的key和已经传过的切片
- 上传，其实只是向后端申请url，因为后端有密钥
- 合并，记录数据库，删redis
- 第一个冲突点是，a用户先查数据库，发现没有，然后在查缓存前，b用户合并完了，接着写库和删redis，最后a用户查redis发现没有，只能重新上传
- 第二个冲突点是，初始化时写进redis value的minio key还没有申请到，如果申请后再写会有重复申请
- 1：正在初始化，相当于打空标记，有点像sync.once；2：正在上传；3：完成，相当于立个墓碑
- 关于ttl，1给10秒，超时不能阻塞必须释放；2给1天，同时minio也删掉一天前的碎片；3给10秒，自动删除
#### 讲讲指标怎么测的
- htop，一个网关监听十个端口，每个端口一万连接
- redis-cli --stat（Redis 请求量统计），一个网关一万连接
- MEMORY USAGE ws:global:202606261430（要执行三次），userid不同
- 每50ms轮询消息队列，群聊消息消费速率下降但落库tps上升，因为protobuf解析 + mqACK + redis申请seqid网络开销 + 数据库网络开销被分摊了
#### 讲讲收割任务（已废弃）
- 首先为什么不用分布式锁，看门狗的话，业务假死不好处理，不用看门狗的话，过期未执行完又有冲突
- 而且在一个服务扫描完投递的途中，有新消息入库暂时处理不过来
- 第一层保障是outbox的标记位，0未投递，1正在投递，2投递失败，重点是update returning和select for update skip locked，然后对2、长时间卡在1的情况做特殊处理
- 第二层保障是幂等落库
- 本质上是同步+异步，为了把分布式事务聚合成单数据库事务
- 后续可以考虑用cdc组件去优化
### 项目介绍二
- 虽然在业务上是一个出行规划的应用，但在底层的定位上是一个面向c端高并发场景的agent运行时，希望通过具体场景去实践自己对agent harness的一些理解
- 从agent工程角度来看，就是多agent的编排，怎样避免竞态，还有工具调用，rag，怎样评估检索召回的质量
- 从后端工程的角度来看，就是上下文怎么存储和加载，以及分布式系统整体的限流
#### 讲讲用户打断
- turnId是每个turn的第一条msgId，即user prompt，msgId是雪花id，目的是在落库前有序
- map<turnId, AbortController>
- 打断时拿runId改AbortController，loop中检查AbortController
#### 讲讲任务状态流转
- createTodoList，updateTodoList（pending->progressing，finish->progressing）,createSubAgent（pending->progressing）
- todolist受压缩保护
#### 讲讲两级限流
- incr/decr
- zset（uuid member+时间戳score），先ZREMRANGEBYSCORE过滤，再统计求和，如果没超就ZADD，超了就拒绝
- 顺序：模型熔断，系统限流（就算后续用户级限流了，也有decr兜底），用户限流
#### 讲讲熔断器
- state，counter，open_until，probes_sent，probes_ok
- 四个lua脚本
- 分用户请求熔断（只读，open时拒绝），和还在跑的模型请求熔断（判断是否open转half-open，是否放行，open_until+probes_sent；请求成功或失败，close时成功一次counter清零，half-open时失败一次probes_ok清零）
#### 讲讲三层上下文存储
- 首先用redis是优化响应速度，ttl为30分钟
- redis里存user prompt，todolist，tool call，model output，summary
- 用userId:seesionId:hash，key是msgId，value是json，避免scan
- 从数据库里加载出来时，先根据summary把压缩过的上下文删掉，再重新估算token
- 数据库是为了重建上下文
- 数据库用msgId去重
- 冷层是跨session的长期记忆，参考了mem0框架的增删改查
- 但后来发现垂直领域下，向量化效果不好，不知道怎么提炼，查询时也慢而且不准确，所以用了pgsql jsonb槽位
- 攒批一个是为了减少调用成本，另一个是因为llm在长文本下提取用户画像效果会更好
- set里存sessionId-msgId，不用hash存内容是避免存两份内容，而且只存user prompt和final output的消息号
- 如果存时数量返回1就开始攒批
- 原本是用定时任务，但考虑到单点故障，就改为mq消息过期时间+死信队列做延迟队列
#### 讲讲压缩
- 在redis里拿到最后一个turnId的最后一个msgId的token count，即final output，加上新一轮的user prompt估算，大于70%就压缩
- 压缩时保留近10条turn+todolist
- 前端展示正在压缩
- 压缩完会根据start msgId和end msgId把redis中的上下文删掉
#### 讲讲工具调用
- 上面三个工具+tavilySearch
- createTodoList和updateTodoList返回成功，然后在user prompt下加todolist，同时给前端展示
- createSubAgent和tavilySearch阻塞等待，前端展示正在运行子agent或者正在网络搜索
#### 讲讲指标怎么测的
- rag测试分为两种，一种是检索，一种是生成
- 检索有两种指标，包含答案的相关文档能否排在检索结果的前面（hit@5，MMR），在所有标准答案中召回了多少（Recall）
- 生成也有两种指标，所有的回答都必须能在检索到的文档中找到依据，回答直接且精准地解决了用户的query
- 证明引入tavily的效果
- 这里的垂类场景具有强时效性，必须人工写死时间锚点和事实，做一次性测试
- 后续可以去做快照测试验证生成
### 反问
- 团队的业务，技术栈，规模，如果能加入的话，我能够负责的部分
- 我需要补充或提升自己哪一方面